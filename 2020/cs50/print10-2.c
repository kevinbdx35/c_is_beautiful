#include <stdio.h>

int main(void)
{
	for (int i = 0; i <= 10; i++)
	 {
		  printf("i vaut maintenant %i: ", i); //ceci n'est pas le but du programme, mais juste un diagnostique temporaire pour nous aider à comprendre ce qui se passe
			printf("#\n");
	 }
}
